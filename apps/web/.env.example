## Frontend (apps/web)
# Toggle sumber data:
# - true  => pakai mockApi (default aman untuk dev)
# - false => pakai backend Express (cookie session)
VITE_USE_MOCK=true

# Base URL backend (tanpa trailing slash).
VITE_API_BASE_URL=http://localhost:8000

